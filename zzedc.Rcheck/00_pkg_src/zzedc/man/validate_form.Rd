% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/form_validators.R, R/validation_cache.R
\name{validate_form}
\alias{validate_form}
\title{Form Validation and Processing Utilities}
\usage{
validate_form(form_data)

validate_form(form_data)
}
\arguments{
\item{form_data}{List of form field values}

\item{field_metadata}{List defining validation rules for each field}
}
\value{
List with:
\itemize{
\item valid: logical, TRUE if all validations passed
\item errors: named list of field-specific error messages
\item warnings: named list of field-specific warnings
\item cleaned_data: validated and cleaned data
}

List with:
\itemize{
\item valid: TRUE if all validations pass, FALSE otherwise
\item errors: List of field-level validation errors (field_name = error_message)
\item field_results: Full results for each field (field_name = TRUE or error message)
}
}
\description{
Server-side validation functions for form submissions
Ensures data quality and regulatory compliance
Validate entire form submission

Validates all provided form values against their cached rules.
}
\details{
Validates all form fields against metadata rules.
Returns detailed validation results suitable for user feedback.
}
\examples{
\dontrun{
metadata <- list(
  age = list(type = "numeric", required = TRUE, min = 18, max = 120),
  email = list(type = "email", required = TRUE)
)

result <- validate_form(
  list(age = 25, email = "user@example.com"),
  metadata
)

if (result$valid) {
  # Process the cleaned data
  save_record(result$cleaned_data)
} else {
  # Show errors to user
  show_validation_errors(result$errors)
}
}
}
