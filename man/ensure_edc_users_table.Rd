% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_users.R
\name{ensure_edc_users_table}
\alias{ensure_edc_users_table}
\title{Create the canonical \code{edc_users} table if it does not exist}
\usage{
ensure_edc_users_table(conn)
}
\arguments{
\item{conn}{A DBI connection (or pool object that delegates
DBI methods).}
}
\value{
Invisible \code{NULL}.
}
\description{
Mirrors the SQLite shape produced by
\code{\link[=create_core_tables]{create_core_tables()}} for the \code{sqlite} backend. The schema
is hard-coded here rather than delegated to
\code{create_core_tables()} so callers that already hold a raw
DBI connection (the C2 importer, the gsheets setup) do not
need to instantiate a \code{DatabaseAdapter} just to provision
one table.
}
\details{
Idempotent: calling this on a database where \code{edc_users}
already exists is a no-op.
}
\keyword{internal}
