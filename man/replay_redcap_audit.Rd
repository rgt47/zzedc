% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_import.R
\name{replay_redcap_audit}
\alias{replay_redcap_audit}
\title{Replay a REDCap audit log into ZZedc's hash-chained audit table.}
\usage{
replay_redcap_audit(redcap_audit, db_path, key, completeness = "full")
}
\arguments{
\item{redcap_audit}{Data frame of REDCap audit rows in the
shape returned by \code{\link[=redcap_extract_audit]{redcap_extract_audit()}} (columns
\code{log_event_id}, \code{ts}, \code{user}, \code{event}, \code{description},
\code{pk}).}

\item{db_path}{Path to the ZZedc database to populate.}

\item{key}{Encryption key (from
\code{initialize_encrypted_database()$key}).}

\item{completeness}{One of \code{"full"}, \code{"partial"}, \code{"empty"}.
When not \code{"full"}, a marker event is prepended to the
audit chain documenting the gap; downstream chain
validation continues to succeed, but the chain is
labelled as starting at the migration boundary rather
than at the project's true genesis.}
}
\value{
List with \code{success}, \code{imported}, \code{errors},
\code{chain_validates}, and (when applicable) \code{completeness}
and \code{marker_inserted}.
}
\description{
Iterates over a REDCap \code{redcap_log_event} extract in
chronological order, computes a ZZedc \code{audit_log} row for
each REDCap row, and writes both that row and a corresponding
\code{audit_chain} row. The hash chain is computed live: each
record's \code{audit_hash} is \code{sha256(event_type | table_name | record_id | operation | details | user_id | timestamp | previous_hash)}, with the first record's \code{previous_hash} set
to \code{GENESIS}. After replay, \code{verify_audit_log_integrity()}
validates the chain.
}
\details{
Imported rows have \code{details} JSON marked
\verb{\{"source":"redcap","redcap_log_event_id":...,"original":"..."\}}
so that REDCap-origin actions remain attributable after the
import.
}
\keyword{internal}
