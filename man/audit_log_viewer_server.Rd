% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_log_viewer_module.R
\name{audit_log_viewer_server}
\alias{audit_log_viewer_server}
\title{Audit Log Viewer Server}
\usage{
audit_log_viewer_server(id, db_pool = NULL)
}
\arguments{
\item{id}{The namespace id for the module}

\item{db_pool}{Reactive expression returning database connection pool}
}
\value{
Invisible NULL
}
\description{
Audit Log Viewer Server
}
\examples{
\dontrun{
# Embed the legacy audit-log viewer (server-only) in a custom app.
# Most users will run the full application via
# `zzedc::launch_zzedc()`; the example below shows how to
# embed the module in a custom Shiny app instead.
if (interactive()) {
  ui <- shiny::fluidPage(
    audit_log_viewer_server("module_id")
  )
  server <- function(input, output, session) {
  }
  shiny::shinyApp(ui, server)
}
}
}
\keyword{internal}
