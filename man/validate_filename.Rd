% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_utils.R
\name{validate_filename}
\alias{validate_filename}
\title{Input Validation Utilities for ZZedc}
\usage{
validate_filename(filename, max_length = 100)
}
\arguments{
\item{filename}{Character string containing the filename}

\item{max_length}{Maximum length for filename (default 100)}
}
\value{
Sanitized filename safe for file operations
}
\description{
Provides secure input validation functions for user-supplied data
to prevent security vulnerabilities like SQL injection and path traversal.
}
\details{
SECURITY CONTEXT:
These functions are critical for preventing OWASP Top 10 vulnerabilities:
\itemize{
\item A1: SQL Injection (through validate_table_name, validate_form_field)
\item A3: Broken Authentication (through password field validation)
\item A4: Sensitive Data Exposure (through secure handling of user inputs)
\item A5: Broken Access Control (through role and resource validation)
\item A7: Cross-Site Scripting - XSS (through form field escaping)
}

COMPLIANCE REQUIREMENTS:
\itemize{
\item GDPR Article 32: Technical and organizational measures to ensure data security
\item 21 CFR Part 11: Electronic Signatures / Input validation requirements
\item HIPAA: Access controls and audit logging (paired with audit_logger.R)
}

All validation must happen at SYSTEM BOUNDARIES (user input, external APIs)
Internal code relies on these validated inputs and trusts internal data structures.
Validate and sanitize filename

Removes path components and special characters to prevent path traversal attacks.
}
\examples{
validate_filename("my-export_2024-01-01.csv")  # Returns: "my-export_2024-01-01.csv"
validate_filename("../../../etc/passwd")       # Returns: "passwd"
}
