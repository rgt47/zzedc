% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_users.R
\name{db_insert_user}
\alias{db_insert_user}
\title{Insert a single user row into \code{edc_users}}
\usage{
db_insert_user(
  conn,
  username,
  password_hash,
  full_name = NULL,
  email = NULL,
  role = "Coordinator",
  site_id = "001",
  active = TRUE,
  created_by = "programmatic",
  user_id = NULL
)
}
\arguments{
\item{conn}{A DBI connection (or pool object).}

\item{username}{Unique username string.}

\item{password_hash}{Pre-hashed password (typically
\code{digest::digest(paste0(password, salt), algo = "sha256")}).}

\item{full_name, email, role, site_id}{Optional columns; all
default to sensible blanks where the canonical schema
permits.}

\item{active}{\code{TRUE} / \code{FALSE} or an integer flag; coerced
to \code{0L} / \code{1L} for the database.}

\item{created_by}{String identifying the path that created
the user (e.g., \code{"setup_wizard"}, \code{"gsheets_setup"},
\code{"redcap_import"}, \code{"admin_ui"}).}

\item{user_id}{Optional pre-generated identifier; auto-
generated if \code{NULL}.}
}
\value{
Named list with \code{success} (logical) and either
\code{user_id} (on success) or \code{error} (the DBI error message).
}
\description{
All callers that previously open-coded the same INSERT now
route through here. The helper does no password hashing of
its own; callers pass a pre-hashed \code{password_hash}. This
keeps each caller's salt strategy under its own control
(the Shiny UI uses \code{Sys.getenv("ZZEDC_SALT")} with a
fallback; the REDCap importer uses an importer-specific
placeholder; the gsheets setup uses its own placeholder).
}
\details{
If \code{user_id} is \code{NULL}, a fresh \verb{USER_<unix_seconds>}
identifier is generated. Callers that need a deterministic
ID (e.g., the Shiny admin UI generating IDs in \code{mode = "add"}) can pass one explicitly.
}
\keyword{internal}
