% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_users.R
\name{db_users}
\alias{db_users}
\title{Internal helpers for the \code{edc_users} table}
\description{
These helpers exist to remove duplication across three
callers that each need to provision and populate \code{edc_users}
programmatically:
}
\details{
\itemize{
\item the Shiny admin UI helper \code{\link[=save_user_to_db]{save_user_to_db()}};
\item \code{\link[=setup_zzedc_from_gsheets]{setup_zzedc_from_gsheets()}} (the Google Sheets
authoring path);
\item \code{\link[=import_redcap_to_zzedc_db]{import_redcap_to_zzedc_db()}} (the REDCap migration
importer, both \code{source = "db"} and \code{source = "api"}).
}

Before this consolidation each caller carried its own
\verb{CREATE TABLE IF NOT EXISTS} and \code{INSERT} statements with
subtly different column lists, leading to schema drift
(\verb{user_id INTEGER PRIMARY KEY AUTOINCREMENT} in the
programmatic callers vs \verb{user_id TEXT PRIMARY KEY} produced
by the canonical \code{create_core_tables()} path used by the
setup wizard). The helpers below adopt the canonical
\code{create_core_tables()} schema as the single source of truth.
}
\keyword{internal}
