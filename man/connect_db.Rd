% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_connection.R
\name{connect_db}
\alias{connect_db}
\title{Connect to Database}
\usage{
connect_db(config = NULL, config_path = "./config.yml")
}
\arguments{
\item{config}{Configuration list (optional)}

\item{config_path}{Path to config.yml (optional)}
}
\value{
DBI connection object
}
\description{
Creates a database connection using the configured backend.
This is the primary connection function for application code.
}
\details{
This function provides a unified interface for database connections:
\itemize{
\item Reads backend configuration from config.yml or environment
\item Creates appropriate connection (SQLite, DuckDB, or PostgreSQL)
\item Handles encryption for SQLite if configured
}
}
\examples{
\dontrun{
  conn <- connect_db()
  result <- DBI::dbGetQuery(conn, "SELECT * FROM subjects")
  disconnect_db(conn)
}

}
\keyword{internal}
