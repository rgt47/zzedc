% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheets_monitor.R
\name{poll_gsheets_and_stage}
\alias{poll_gsheets_and_stage}
\title{Poll Google Sheet and Stage Proposed Changes}
\usage{
poll_gsheets_and_stage(
  sheet_id,
  db_path = NULL,
  staged_by = "system_monitor",
  sheet_name = "Study_Users",
  notifier = default_proposal_notifier
)
}
\arguments{
\item{sheet_id}{Google Sheets ID or full URL.}

\item{db_path}{Path to the ZZedc SQLite database. \code{NULL} uses
\code{get_db_path()}.}

\item{staged_by}{Username recorded on each staged proposal.}

\item{sheet_name}{Name of the validation-rules tab in the workbook.}

\item{notifier}{A function that accepts a data frame of pending
proposals and sends a notification. Defaults to
\code{\link[=default_proposal_notifier]{default_proposal_notifier()}}, which writes to a log file.
Replace with a function that sends email for production use
(see the technical guide for a \code{blastula} example).}
}
\value{
Invisibly returns a list with \code{new}, \code{modified}, \code{deleted}
counts and \code{staged} count. Returns \code{NULL} silently if no changes
were found.
}
\description{
Combines \code{\link[=diff_gsheets_rules]{diff_gsheets_rules()}}, \code{\link[=stage_gsheets_proposals]{stage_gsheets_proposals()}}, and
notification into a single call suitable for a scheduled cron job.
When no changes are detected the function returns silently without
calling the notifier.
}
\examples{
\dontrun{
poll_gsheets_and_stage(
  sheet_id  = 'https://docs.google.com/spreadsheets/d/YOUR_ID',
  db_path   = '~/prj/praz-large/data/praz-large.db',
  staged_by = 'system_monitor'
)
}

}
