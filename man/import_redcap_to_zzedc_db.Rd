% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_import.R
\name{import_redcap_to_zzedc_db}
\alias{import_redcap_to_zzedc_db}
\title{Direct-to-database REDCap importer (Phase C2 + C3a)}
\usage{
import_redcap_to_zzedc_db(
  conn = NULL,
  pid = NA,
  db_path,
  source = c("db", "api"),
  api = NULL,
  api_url = NULL,
  api_token = NULL,
  overwrite = FALSE,
  on_conflict = c("skip", "merge", "fail"),
  dry_run = FALSE,
  imported_by = "redcap_import"
)
}
\arguments{
\item{conn}{DBI connection to a REDCap-shaped database.
Required for \code{source = "db"}.}

\item{pid}{Numeric REDCap project ID. Required for
\code{source = "db"}; ignored for \code{source = "api"} (the API
token implicitly scopes the project).}

\item{db_path}{Path to the ZZedc encrypted SQLite database
to create. Must not exist unless \code{overwrite = TRUE}.}

\item{source}{One of \code{"db"} (default) or \code{"api"}.}

\item{api}{Optional pre-built API handle from
\code{\link[=redcap_api_connect]{redcap_api_connect()}}. If supplied, \code{api_url} /
\code{api_token} are ignored. Useful for tests.}

\item{api_url}{REDCap API endpoint. Required for
\code{source = "api"} when \code{api} is not supplied.}

\item{api_token}{REDCap API token. Required for
\code{source = "api"} when \code{api} is not supplied.}

\item{overwrite}{Logical. Overwrite an existing database?
Default \code{FALSE}.}

\item{on_conflict}{Resolution policy when imported rows
conflict with existing rows (relevant only when importing
into a non-empty database). One of \code{"skip"} (default),
\code{"merge"} (more permissive role wins), \code{"fail"}.}

\item{dry_run}{Logical. If TRUE, exercises the importer
end-to-end but rolls back the database transaction.}

\item{imported_by}{User ID recorded for the import action.}
}
\value{
Named list with one entry per stage (\code{csv},
\code{database}, \code{users}, \code{forms}, \code{subjects}, \code{subject_data},
\code{audit_replay}), each carrying its own success flag and
counts. Top-level \code{success} is TRUE only if every stage
passed. Includes \code{key} (encryption key) so callers can
\code{Sys.setenv(DB_ENCRYPTION_KEY = result$key)} for subsequent
operations. The \code{audit_replay} slot carries
\code{completeness} (\code{"full"} / \code{"partial"} / \code{"empty"}) and
\code{marker_inserted} (TRUE iff a \code{MIGRATION_AUDIT_GAP} marker
was prepended).
}
\description{
One-call equivalent to the C1 two-step flow: extract from
REDCap, create a ZZedc database, populate users and CRFs,
load subjects and form data, and replay the REDCap audit log
into the ZZedc hash-chained audit trail.
}
\details{
Two source modes are supported:
\itemize{
\item \code{source = "db"} (the default; Phase C2): extract via
a live DBI connection (typically MySQL / MariaDB
against the REDCap relational tables). Audit log is
always full.
\item \code{source = "api"} (Phase C3a): extract via the REDCap
REST API using \pkg{REDCapR}. The audit log is
classified as \code{full}, \code{partial}, or \code{empty} depending
on the token's \code{Logging} permission. When not
\code{"full"}, a \code{MIGRATION_AUDIT_GAP} marker is prepended
to the audit chain so continuity-of-record is
preserved from the migration boundary forward.
}

Phase C2 / C3a limitations (deferred to subsequent work):
\itemize{
\item Repeating instruments and longitudinal events are
flattened into a single \code{visit_code} per
\code{event_id}. Studies with repeating-instrument
schemas should review the import diagnostics and may
need to restructure the data dictionary.
\item Branching-logic rules that exceed the simple grammar
in \code{redcap_translate_branching_logic()} are recorded
as skipped rather than translated; manual review is
required.
\item \code{.sql}-dump source mode is Phase C3b.
}
}
\examples{
\dontrun{
# Phase C2 (live MySQL source)
library(RMariaDB)
conn <- DBI::dbConnect(MariaDB(),
  host     = "redcap.example.org",
  dbname   = "redcap",
  user     = "redcap_reader",
  password = Sys.getenv("REDCAP_DB_PASSWORD"))
on.exit(DBI::dbDisconnect(conn), add = TRUE)

result <- import_redcap_to_zzedc_db(
  conn       = conn,
  pid        = 42L,
  db_path    = "/srv/zzedc/MIGRATED-001/study.db",
  overwrite  = TRUE
)

# Phase C3a (REST API source)
result_api <- import_redcap_to_zzedc_db(
  source    = "api",
  api_url   = "https://redcap.example.org/api/",
  api_token = Sys.getenv("REDCAP_API_TOKEN"),
  db_path   = "/srv/zzedc/MIGRATED-002/study.db",
  overwrite = TRUE
)

Sys.setenv(DB_ENCRYPTION_KEY = result$key)
result$audit_replay$chain_validates    # should be TRUE
result$audit_replay$completeness       # full / partial / empty
result$audit_replay$marker_inserted    # TRUE if not full
length(result$skipped_rules)           # rules needing manual review
}

}
\seealso{
\code{\link[=import_redcap_to_zzedc]{import_redcap_to_zzedc()}} for Phase C1 (CSV
emission); \code{\link[=redcap_api_connect]{redcap_api_connect()}} for the API handle;
\code{vignette('mysql-redcap-migration-roadmap')} for the full
phasing.
}
