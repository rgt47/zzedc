% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheets_monitor.R
\name{stage_gsheets_proposals}
\alias{stage_gsheets_proposals}
\title{Stage Proposed Rule Changes}
\usage{
stage_gsheets_proposals(
  diff,
  sheet_id,
  db_path = NULL,
  staged_by = "system_monitor"
)
}
\arguments{
\item{diff}{Output of \code{\link[=diff_gsheets_rules]{diff_gsheets_rules()}}.}

\item{sheet_id}{Google Sheets ID (stored with each proposal for
traceability).}

\item{db_path}{Path to the ZZedc SQLite database. \code{NULL} uses
\code{get_db_path()}.}

\item{staged_by}{Username of the account performing the staging
(typically the cron service account, e.g. \code{'system_monitor'}).}
}
\value{
A list with \code{staged} (count of proposals written) and
\code{skipped} (count already in \code{PENDING} with identical content).
}
\description{
Writes each row in \code{diff} to the \code{dsl_rule_proposals} table with
\code{status = 'PENDING'}. Live rules in \code{dsl_validation_rules} are not
modified. Any rule already in \code{PENDING} state for the same \code{rule_id}
is updated in place rather than duplicated.
}
\keyword{internal}
