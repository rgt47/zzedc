% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheets_monitor.R
\name{approve_user_proposals}
\alias{approve_user_proposals}
\title{Approve Pending User Proposals}
\usage{
approve_user_proposals(ids, reviewer_id, db_path = NULL, comments = NULL)
}
\arguments{
\item{ids}{Character vector of \code{proposal_id} values to approve.}

\item{reviewer_id}{Username of the approving manager.}

\item{db_path}{Path to the ZZedc SQLite database. \code{NULL} uses
\code{get_db_path()}.}

\item{comments}{Optional comments recorded on each approval.}
}
\value{
A list with \code{approved} (count) and \code{errors}.
}
\description{
For each \code{proposal_id} in \code{ids}: inserts or updates the user in
\code{edc_users} from the proposal record, marks the proposal
\code{'APPROVED'}, and writes an audit entry. Passwords for \code{NEW} users
are set to a secure random initial value that the coordinator must
change on first login; the hash is recorded but never returned.
}
