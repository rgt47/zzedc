% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encryption_utils.R
\name{test_encryption}
\alias{test_encryption}
\title{Test database encryption}
\usage{
test_encryption(db_path, key)
}
\arguments{
\item{db_path}{Character: Path to test database file (will be created and deleted)}

\item{key}{Character: Encryption key to test}
}
\value{
Logical TRUE if all tests pass, otherwise stops with error
}
\description{
Creates a test encrypted database, writes data, reads it back,
and verifies encryption is actually being applied (file is binary, not plaintext).
}
\details{
Test procedure:
\enumerate{
\item Connect to database with key
\item Write test data
\item Disconnect
\item Verify file is encrypted (random bytes, not readable text)
\item Reconnect with correct key -> data readable
\item Verify data integrity
\item Cleanup temporary database
}

This function verifies that SQLCipher is properly compiled into RSQLite.
}
\examples{
\dontrun{
  key <- generate_db_key()
  test_encryption(tempfile(fileext = ".db"), key)
}

}
