% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_pagination.R
\name{filter_data_by_search}
\alias{filter_data_by_search}
\title{Filter data by search term}
\usage{
filter_data_by_search(data, search_term, columns = NULL)
}
\arguments{
\item{data}{data.frame to search}

\item{search_term}{Text to search for (case-insensitive)}

\item{columns}{Column names to search in (NULL = all columns)}
}
\value{
Filtered data.frame
}
\description{
Searches all columns for matching values
}
\examples{
\dontrun{
# Internal helper used by paginated views in the Shiny app.
# Subset a captured dataset by a free-text query (case-
# insensitive substring match across character columns).
df <- data.frame(
  subject_id = paste0("SUBJ-", sprintf("\%03d", 1:5)),
  arm        = c("A", "A", "B", "B", "A"),
  notes      = c("baseline", "week 4", "baseline",
                 "week 12", "week 4")
)
filter_data_by_search(df, search_term = "week 4")
}
}
\keyword{internal}
