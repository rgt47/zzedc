% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_adapter.R
\name{create_db_adapter}
\alias{create_db_adapter}
\title{Create Database Adapter from Configuration}
\usage{
create_db_adapter(config)
}
\arguments{
\item{config}{Configuration list containing at minimum:
\itemize{
\item db_backend: One of "sqlite", "duckdb", "postgresql", "postgres",
"clickhouse"
\item Backend-specific settings (see individual adapter documentation)
}}
}
\value{
DatabaseAdapter subclass instance
}
\description{
Factory function that creates the appropriate database adapter based on
the db_backend setting in the configuration.
}
\details{
Supported backends:
\itemize{
\item "sqlite": Creates SQLiteAdapter, requires sqlite$path
\item "duckdb": Creates DuckDBAdapter, requires duckdb$path
\item "postgresql" or "postgres": Creates PostgreSQLAdapter, requires
postgresql$host, postgresql$name, postgresql$user, postgresql$password
\item "clickhouse": Creates ClickHouseAdapter, requires clickhouse$host,
clickhouse$database
}
}
\examples{
\dontrun{
  # SQLite configuration
  config <- list(
    db_backend = "sqlite",
    sqlite = list(path = "data/zzedc.db")
  )
  adapter <- create_db_adapter(config)

  # DuckDB configuration
  config <- list(
    db_backend = "duckdb",
    duckdb = list(path = "data/zzedc.duckdb")
  )
  adapter <- create_db_adapter(config)

  # PostgreSQL configuration
  config <- list(
    db_backend = "postgresql",
    postgresql = list(
      host = "localhost",
      port = 5432,
      name = "zzedc",
      user = "zzedc_user",
      password = "secret"
    )
  )
  adapter <- create_db_adapter(config)

  # ClickHouse configuration
  config <- list(
    db_backend = "clickhouse",
    clickhouse = list(
      host = "localhost",
      port = 8123,
      database = "zzedc",
      user = "default"
    )
  )
  adapter <- create_db_adapter(config)
}

}
