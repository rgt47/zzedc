% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_migration.R
\name{migrate_between_backends}
\alias{migrate_between_backends}
\title{Migrate Database Between Backends}
\usage{
migrate_between_backends(
  source_config,
  dest_config,
  tables = NULL,
  batch_size = 1000,
  verbose = TRUE
)
}
\arguments{
\item{source_config}{Configuration for source database}

\item{dest_config}{Configuration for destination database}

\item{tables}{Character vector of table names to migrate (default: all)}

\item{batch_size}{Number of rows to transfer per batch (default: 1000)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
List with migration results including row counts and any errors
}
\description{
Copies all data from a source database to a destination database,
handling dialect differences automatically.
}
\examples{
\dontrun{
  # Migrate from SQLite to PostgreSQL
  source <- list(db_backend = "sqlite",
                 sqlite = list(path = "data/zzedc.db"))
  dest <- list(db_backend = "postgresql",
               postgresql = list(host = "localhost", name = "zzedc",
                                 user = "admin", password = "secret"))

  result <- migrate_between_backends(source, dest)
}

}
