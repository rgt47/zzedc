% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_enhanced.R
\name{detect_audit_anomalies}
\alias{detect_audit_anomalies}
\title{Detect Audit Anomalies}
\usage{
detect_audit_anomalies(lookback_hours = 24, thresholds = NULL, db_path = NULL)
}
\arguments{
\item{lookback_hours}{Numeric: Hours to analyze (default: 24)}

\item{thresholds}{List: Custom thresholds for anomaly detection}

\item{db_path}{Character: Database path (optional)}
}
\value{
List with detected anomalies and risk assessment
}
\description{
Analyzes audit trail for suspicious patterns and anomalies.
}
\details{
Detects the following anomaly types:
\itemize{
\item Excessive failed logins (possible brute force)
\item Unusual access patterns (off-hours activity)
\item Bulk data operations (possible data exfiltration)
\item Multiple role changes (possible privilege escalation)
\item Integrity verification failures
}
}
\examples{
\dontrun{
  anomalies <- detect_audit_anomalies(lookback_hours = 24)
  if (length(anomalies$alerts) > 0) {
    print(anomalies$alerts)
  }
}

}
