% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/home_module.R
\name{home_ui}
\alias{home_ui}
\title{Home Module UI}
\usage{
home_ui(id)
}
\arguments{
\item{id}{The namespace id for the module}
}
\value{
A tagList containing the home dashboard UI
}
\description{
Home Module UI
}
\examples{
\dontrun{
# Embed the study home / overview dashboard.
# Most users will run the full application via
# `zzedc::launch_zzedc()`; the example below shows how to
# embed the module in a custom Shiny app instead.
if (interactive()) {
  ui <- shiny::fluidPage(
    home_ui("module_id")
  )
  server <- function(input, output, session) {
    home_server("module_id")
  }
  shiny::shinyApp(ui, server)
}
}
}
\keyword{internal}
