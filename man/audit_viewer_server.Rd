% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_viewer_module.R
\name{audit_viewer_server}
\alias{audit_viewer_server}
\title{Audit Viewer Server}
\usage{
audit_viewer_server(id, db_path = shiny::reactive(NULL))
}
\arguments{
\item{id}{Character: Module namespace ID}

\item{db_path}{Reactive: Database path}
}
\value{
Module server function
}
\description{
Server logic for the audit trail viewer module.
}
\examples{
\dontrun{
# Embed the audit-trail viewer module in a custom Shiny app.
# Most users will run the full application via
# `zzedc::launch_zzedc()`; the example below shows how to
# embed the module in a custom Shiny app instead.
if (interactive()) {
  ui <- shiny::fluidPage(
    audit_viewer_ui("module_id")
  )
  server <- function(input, output, session) {
    audit_viewer_server("module_id")
  }
  shiny::shinyApp(ui, server)
}
}
}
