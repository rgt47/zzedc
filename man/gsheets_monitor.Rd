% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheets_monitor.R
\name{gsheets_monitor}
\alias{gsheets_monitor}
\title{Google Sheets Change Monitor}
\description{
Polls the \code{Study_Users} tab of each site's Google Sheets workbook
for coordinator edits, stages proposed changes in the database
without modifying the live user roster, notifies the study manager,
and applies approved changes on the manager's instruction.
}
\details{
\subsection{Access model}{

Each site coordinator holds editor access to the \code{Study_Users} tab
of their site's workbook only. The \code{Data_Dictionary} and
\code{validation_rules} tabs are viewer-only for coordinators (enforced
via Google Sheets tab protection). This file implements the
change-detection and approval layer for \code{Study_Users} edits.

The validation-rule diff/stage/approve functions
(\code{\link[=diff_gsheets_rules]{diff_gsheets_rules()}}, \code{\link[=stage_gsheets_proposals]{stage_gsheets_proposals()}},
\code{\link[=approve_proposals]{approve_proposals()}}, \code{\link[=reject_proposals]{reject_proposals()}}) are retained for
data-scientist-initiated rule changes and are not part of the
coordinator-facing workflow.
}

\subsection{Workflow}{
\enumerate{
\item A cron job calls \code{\link[=poll_gsheets_and_stage]{poll_gsheets_and_stage()}} for each site workbook.
\item The function reads the \code{Study_Users} tab, diffs it against the
live \code{edc_users} table, and writes any new or changed rows to
the \code{user_proposals} staging table with \code{status = 'PENDING'}.
The live user roster is not touched.
\item The notifier function is called with a data frame of pending
proposals. The default notifier writes a plain-text summary to a
log file; supply a custom function to send email.
\item The manager reviews proposals and calls \code{\link[=approve_user_proposals]{approve_user_proposals()}}
or \code{\link[=reject_user_proposals]{reject_user_proposals()}}. Approval upserts the user into
\code{edc_users}; rejection records the decision and leaves the roster
unchanged.
}
}
}
