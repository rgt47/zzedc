% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_wizard_utils.R
\name{create_wizard_database}
\alias{create_wizard_database}
\title{Create ZZedc Database from Wizard Configuration}
\usage{
create_wizard_database(
  config_list,
  db_path = NULL,
  backend = "sqlite",
  pg_config = NULL
)
}
\arguments{
\item{config_list}{List containing wizard configuration}

\item{db_path}{Path where database file will be created (for SQLite/DuckDB)}

\item{backend}{Database backend: "sqlite" (default), "duckdb", or "postgresql"}

\item{pg_config}{PostgreSQL configuration list (required if backend="postgresql")}
}
\value{
List with success status and messages
}
\description{
Creates a complete database with all required tables.
Supports SQLite (default), DuckDB, and PostgreSQL backends.
}
\examples{
\dontrun{
config <- list(
  study_name = "My Study",
  protocol_id = "PROTO-001",
  admin_username = "admin",
  admin_password = "MyPass123!",
  security_salt = "abc123..."
)

# SQLite (default)
create_wizard_database(config, "~/my_study.db")

# DuckDB
create_wizard_database(config, "~/my_study.duckdb", backend = "duckdb")

# PostgreSQL
create_wizard_database(config, backend = "postgresql",
  pg_config = list(host = "localhost", name = "zzedc",
                   user = "admin", password = "secret"))
}
}
