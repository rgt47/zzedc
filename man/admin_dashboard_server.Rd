% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_dashboard_module.R
\name{admin_dashboard_server}
\alias{admin_dashboard_server}
\title{Admin Dashboard Server}
\usage{
admin_dashboard_server(
  id,
  db_pool = NULL,
  user_session = NULL,
  db_path = reactive("./data/zzedc.db")
)
}
\arguments{
\item{id}{The namespace id for the module}

\item{db_pool}{Reactive expression returning database connection pool}

\item{user_session}{Reactive values with authenticated user info}

\item{db_path}{Reactive expression returning path to database file}
}
\value{
Invisible NULL
}
\description{
Admin Dashboard Server
}
\examples{
\dontrun{
# Mount the administrator dashboard's server logic.
# Most users will run the full application via
# `zzedc::launch_zzedc()`; the example below shows how to
# embed the module in a custom Shiny app instead.
if (interactive()) {
  server <- function(input, output, session) {
    admin_dashboard_server("module_id")
  }
  shiny::shinyApp(shiny::fluidPage(), server)
}
}
}
\keyword{internal}
