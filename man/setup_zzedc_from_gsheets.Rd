% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_gsheets_integration.R
\name{setup_zzedc_from_gsheets}
\alias{setup_zzedc_from_gsheets}
\title{Set up a ZZedc study from a Google Sheets workbook}
\usage{
setup_zzedc_from_gsheets(
  sheet_id,
  db_path,
  tabs = list(users = "Study_Users", data_dictionary = "Data_Dictionary", validation =
    "validation_rules"),
  imported_by = "setup_from_gsheets",
  overwrite = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{sheet_id}{Google Sheets ID or URL.}

\item{db_path}{Path to the encrypted SQLite database to
create. Required.}

\item{tabs}{Named list of tab names. Defaults are
\code{Study_Users}, \code{Data_Dictionary}, and \code{validation_rules}.
Override entries to match the workbook's tab layout.}

\item{imported_by}{User ID recorded in the audit trail for the
import. Defaults to \code{"setup_from_gsheets"}.}

\item{overwrite}{Logical. If \code{TRUE}, an existing database at
\code{db_path} is overwritten. Defaults to \code{FALSE}.}

\item{dry_run}{Logical. If \code{TRUE}, parses and validates the
workbook without writing to the database. Defaults to
\code{FALSE}.}
}
\value{
List with one element per stage (\code{database}, \code{users},
\code{forms}, \code{validation_rules}), each carrying its own
\code{success} flag, counts, and error list. The top-level
\code{success} is \code{TRUE} only if every stage succeeded. The
encryption key (\code{key}) is included so the caller can
register it with \code{Sys.setenv(DB_ENCRYPTION_KEY = ...)}
before subsequent operations.
}
\description{
One-call orchestrator that initialises an encrypted study
database, then imports the user roster, data dictionary
(CRF definitions and fields), and validation rules from a
Google Sheets workbook. Each tab in the workbook corresponds
to one of these artefacts.
}
\details{
The Google Sheet must contain three tabs:

\describe{
\item{Study_Users}{Columns: \code{username}, \code{full_name}, \code{email},
\code{role}, \code{password_initial}. Optional: \code{site_id}, \code{active}.}
\item{Data_Dictionary}{One row per field across all CRFs.
Columns: \code{form_code}, \code{field_name}, \code{field_label},
\code{field_type}, \code{required}. Optional: \code{validation},
\code{description}, \code{field_order}.}
\item{validation_rules}{Columns expected by
\code{\link[=import_validation_rules_from_gsheets]{import_validation_rules_from_gsheets()}}: \code{rule_id},
\code{field_code}, \code{rule_dsl} and the optional columns documented
there.}
}

Bidirectional sync of validation rules is available via
\code{\link[=sync_dsl_rules_from_gsheets]{sync_dsl_rules_from_gsheets()}} for ongoing studies.
}
\examples{
\dontrun{
result <- setup_zzedc_from_gsheets(
  sheet_id = "1AbC...your-sheet-id...",
  db_path  = "~/zzedc_demonstration_trial/data/demonstration_trial.db"
)
Sys.setenv(DB_ENCRYPTION_KEY = result$key)
result$users$imported       # number of users imported
result$forms$imported_forms # number of CRFs created
result$validation_rules$imported  # number of rules imported
}

}
\seealso{
\code{\link[=import_validation_rules_from_gsheets]{import_validation_rules_from_gsheets()}} for importing
validation rules only; \code{\link[=sync_dsl_rules_from_gsheets]{sync_dsl_rules_from_gsheets()}} for
incremental synchronisation.
}
