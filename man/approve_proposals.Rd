% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsheets_monitor.R
\name{approve_proposals}
\alias{approve_proposals}
\title{Approve Pending Proposals}
\usage{
approve_proposals(ids, reviewer_id, db_path = NULL, comments = NULL)
}
\arguments{
\item{ids}{Character vector of \code{proposal_id} values to approve.}

\item{reviewer_id}{Username of the approving manager.}

\item{db_path}{Path to the ZZedc SQLite database. \code{NULL} uses
\code{get_db_path()}.}

\item{comments}{Optional comments recorded on each approval.}
}
\value{
A list with \code{approved} (count) and \code{errors} (named list of
rule IDs that failed with their error messages).
}
\description{
For each \code{proposal_id} in \code{ids}: compiles and writes the proposed DSL
to the live \code{dsl_validation_rules} row (or inserts a new row for
\code{NEW} changes), sets \code{is_active = 1}, and records the approval
decision in \code{dsl_rule_proposals}. The live rule is rebuilt atomically
-- the old compiled R and SQL expressions are replaced with those
derived from the approved DSL.
}
