% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_correction_module.R
\name{data_correction_server}
\alias{data_correction_server}
\title{Data Correction Server}
\usage{
data_correction_server(id, current_user, user_role, db_path = NULL)
}
\arguments{
\item{id}{Module namespace ID}

\item{current_user}{Reactive: Current user ID}

\item{user_role}{Reactive: Current user role}

\item{db_path}{Character: Database path}
}
\value{
Module server
}
\description{
Data Correction Server
}
\examples{
\dontrun{
# Embed the data-correction approval workflow.
# Most users will run the full application via
# `zzedc::launch_zzedc()`; the example below shows how to
# embed the module in a custom Shiny app instead.
if (interactive()) {
  ui <- shiny::fluidPage(
    data_correction_ui("module_id")
  )
  server <- function(input, output, session) {
    data_correction_server("module_id")
  }
  shiny::shinyApp(ui, server)
}
}
}
