% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_import.R
\name{redcap_api_connect}
\alias{redcap_api_connect}
\title{Connect to a REDCap project via the REST API.}
\usage{
redcap_api_connect(api_url = NULL, api_token = NULL, ops = NULL)
}
\arguments{
\item{api_url}{REDCap API endpoint
(e.g., \verb{https://redcap.example.org/api/}).}

\item{api_token}{Project-scoped API token. Tokens carry the
permissions of the user that minted them; for full audit
replay, the token must come from a user with \code{Logging}
rights, otherwise the \code{log} endpoint will return a redacted
or empty response (see \code{redcap_check_audit_completeness()}).}

\item{ops}{Optional list of operation closures; when supplied,
\code{REDCapR} is not invoked. Used by the test suite to inject
synthetic responses.}
}
\value{
List with \code{url}, \code{token}, \code{version}, and \code{ops}.
}
\description{
Validates the URL/token pair by calling the \code{version}
endpoint and returns an opaque handle that the API extractors
below consume. The handle holds an \code{ops} list of zero-argument
(or single-argument) closures that wrap the underlying
\pkg{REDCapR} calls; tests can construct an \code{api} handle with
synthetic \code{ops} directly, bypassing the network.
}
\keyword{internal}
