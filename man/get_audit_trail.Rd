% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_logging.R
\name{get_audit_trail}
\alias{get_audit_trail}
\title{Get Audit Trail}
\usage{
get_audit_trail(filters = list(), include_chain = FALSE, db_path = NULL)
}
\arguments{
\item{filters}{List: Filter criteria (user, table_name, event_type, date_from, date_to)}

\item{include_chain}{Logical: Include hash chain info? (default: FALSE)}

\item{db_path}{Character: Database path (optional)}
}
\value{
Data frame with audit trail records
}
\description{
Retrieve audit trail with optional filtering and verification.
}
\examples{
\dontrun{
  # Get all events for a user
  trail <- get_audit_trail(filters = list(user = "jane_smith"))

  # Get updates to subjects table
  trail <- get_audit_trail(
    filters = list(
      table_name = "subjects",
      event_type = "UPDATE"
    )
  )
}

}
