% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_connection.R
\name{initialize_encrypted_database}
\alias{initialize_encrypted_database}
\title{Initialize Encrypted Database}
\usage{
initialize_encrypted_database(db_path = NULL, overwrite = FALSE, key = NULL)
}
\arguments{
\item{db_path}{Character: Path for new database (optional, uses get_db_path if NULL)}

\item{overwrite}{Logical: Overwrite existing database? (default: FALSE)}

\item{key}{Character. Encryption key to use. If \code{NULL} (default), a
new 256-bit key is generated. Pass an existing key when migrating
or recreating a database with a known key.}
}
\value{
List with initialization results:
\itemize{
\item success: Logical TRUE if successful
\item path: Absolute path to created database
\item key: The encryption key used (either \code{key} if supplied or the
newly generated one). The function does not persist
this key beyond the current function call; the caller must store
it (for example via \code{Sys.setenv(DB_ENCRYPTION_KEY = result$key)}
or AWS Secrets Manager). Losing the key makes the database
unrecoverable.
\item message: Status message
}
}
\description{
Creates a new encrypted database with complete schema.
}
\details{
This function:
\enumerate{
\item Checks if database exists (fails if overwrite=FALSE)
\item Generates random 256-bit encryption key
\item Creates encrypted database connection
\item Creates base tables (study_info, subjects, etc.)
\item Returns the encryption key in the result list
\item Verifies encryption is working
}
}
\examples{
\dontrun{
  result <- initialize_encrypted_database(
    db_path = "./data/new_study.db",
    overwrite = FALSE
  )
  if (result$success) {
    # Store the key for subsequent operations in this session
    Sys.setenv(DB_ENCRYPTION_KEY = result$key)
    cat("Database created at:", result$path, "\n")
  }
}

}
