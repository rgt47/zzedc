% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crf_version.R
\name{create_crf_definition}
\alias{create_crf_definition}
\title{Create CRF Definition}
\usage{
create_crf_definition(
  crf_code,
  crf_name,
  created_by,
  crf_description = NULL,
  crf_category = NULL
)
}
\arguments{
\item{crf_code}{Unique CRF code}

\item{crf_name}{CRF name}

\item{created_by}{User creating the CRF}

\item{crf_description}{Optional description}

\item{crf_category}{Optional category}
}
\value{
List with success status and CRF details
}
\description{
Creates a new CRF definition for version control.
}
\examples{
\dontrun{
# After initialising an encrypted database (see
# `initialize_encrypted_database()`), define a baseline-visit CRF
# for a new study. The returned `crf_id` is the handle used by
# subsequent calls such as `add_form_section()` and
# `add_form_field()`.
crf <- create_crf_definition(
  crf_code        = "BASELINE",
  crf_name        = "Baseline Visit Assessment",
  created_by      = "biostat_lead",
  crf_description = "Demographics, vitals, and consent status",
  crf_category    = "visit_form"
)
if (crf$success) crf$crf_id
}
}
