% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aws_kms_utils.R
\name{setup_aws_kms}
\alias{setup_aws_kms}
\title{Setup AWS KMS Integration for ZZedc}
\usage{
setup_aws_kms()
}
\value{
List with AWS KMS configuration status:
\itemize{
\item aws_configured: Logical TRUE if AWS KMS properly configured
\item region: AWS region (from config or env var)
\item credentials_found: Logical TRUE if AWS credentials available
\item secret_exists: Logical TRUE if default secret exists
\item permissions: List with permission check results
\item errors: Character vector of any setup errors
\item message: Human-readable status message
}
}
\description{
Initializes and validates AWS KMS configuration for production key management.
Checks AWS credentials, region, and permissions before returning setup status.
}
\details{
AWS KMS Setup Requirements:
\enumerate{
\item AWS credentials configured:
\itemize{
\item ~/.aws/credentials file, OR
\item AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY environment variables
}
\item AWS region configured:
\itemize{
\item AWS_REGION environment variable, OR
\item ~/.aws/config file with \link{default} region
}
\item IAM permissions required:
\itemize{
\item secretsmanager:CreateSecret (for initial setup)
\item secretsmanager:GetSecretValue (for key retrieval)
\item secretsmanager:PutSecretValue (for key rotation)
\item secretsmanager:DeleteSecretVersion (for archiving)
}
}

Default secret name: "zzedc/db-encryption-key"
}
\examples{
\dontrun{
  status <- setup_aws_kms()
  if (status$aws_configured) {
    cat("AWS KMS ready for key management\n")
  } else {
    cat("Setup errors:", status$errors, "\n")
  }
}

}
