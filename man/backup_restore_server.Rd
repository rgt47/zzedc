% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backup_restore_module.R
\name{backup_restore_server}
\alias{backup_restore_server}
\title{Backup/Restore Server}
\usage{
backup_restore_server(
  id,
  db_pool = NULL,
  db_path = reactive("./data/zzedc.db"),
  backup_dir = "./backups"
)
}
\arguments{
\item{id}{The namespace id for the module}

\item{db_pool}{Reactive expression returning database connection pool}

\item{db_path}{Reactive expression returning path to database file}

\item{backup_dir}{Directory where backups are stored}
}
\value{
Invisible NULL
}
\description{
Backup/Restore Server
}
\examples{
\dontrun{
# Embed the backup-and-restore module in a custom Shiny app.
# Most users will run the full application via
# `zzedc::launch_zzedc()`; the example below shows how to
# embed the module in a custom Shiny app instead.
if (interactive()) {
  ui <- shiny::fluidPage(
    backup_restore_ui("module_id")
  )
  server <- function(input, output, session) {
    backup_restore_server("module_id")
  }
  shiny::shinyApp(ui, server)
}
}
}
\keyword{internal}
