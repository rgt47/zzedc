% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_import.R
\name{redcap_translate_branching_logic}
\alias{redcap_translate_branching_logic}
\title{Translate a REDCap branching-logic expression to ZZedc DSL.}
\usage{
redcap_translate_branching_logic(expr)
}
\arguments{
\item{expr}{REDCap branching-logic expression.}
}
\value{
Character ZZedc DSL expression, or NA if untranslatable.
}
\description{
REDCap's branching-logic syntax uses \verb{[field] OP value} with
the field name in square brackets. Phase C2 translates the
three most common patterns:
}
\details{
\enumerate{
\item Equality: \verb{[sex] = "F"} -> \code{sex == "F"}
\item Comparison: \verb{[age] >= 18} -> \code{age >= 18}
\item Logical compound: \verb{[a] = "1" or [b] = "1"} -> \code{a == "1" || b == "1"}
}

Phase C2 conservatively returns NA for any expression that
contains a construct it does not recognise (parentheses
nested deeper than one level, REDCap-specific functions like
\code{datediff()}, instance references like \verb{[event_arm_1][age]}).
Such rows are recorded in the importer's
\code{result$skipped_rules} list so the user can translate them
manually following the patterns documented in
\code{vignette('content-author-guide')} Chapter 3 ('Authoring the
workbook').
}
\keyword{internal}
